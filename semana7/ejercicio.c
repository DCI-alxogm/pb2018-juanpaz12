/*este el el programa de la semana 7*/ 
#include<stdio.h>
#include<math.h>

int main (){

FILE *read;
float x, p, inter, n, r;

     printf("hola, este progrma ayuda a evalura la funcion f(x)=x^p, asi como su integral\n");
     printf("por favor, crea un arcivo llamado Valores.txt, el cual contenga el intervalo de la integral (a,b), el valor de p, el valor de x y por ultimo el numero de veces que se evaluara la fincion.\n");
     read=fopen("valores.txt","r");
(b-a)/n;
     
  
